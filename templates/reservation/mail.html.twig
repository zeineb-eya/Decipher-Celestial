{% extends ('baseback/base.html.twig') %}
{% block title %}Mail confermation{% endblock title %}

{% block body %}

    <div>
        {{ form_start(our_form) }}
       
    {{ form_widget(our_form) }}
        
         <button class="btn">{{ button_label|default('Send') }}</button>
        {{ form_end(our_form) }}
    </div>

{% endblock %}