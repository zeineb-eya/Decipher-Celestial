{% extends ('base_back_f/basebackF.html.twig') %}


{% block title %}Post
{% endblock %}

{% block body %}
	<h1>Post</h1>

	<table class="table">
		<tbody>
			<tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>Nom_post</th>
                <td>{{ post.nom }}</td>
            </tr>
            <tr>
                <th>Img_post</th>
                <td><img src={{asset('uploads/images/'~post.getImgPost())}}></td>
            </tr>
            <tr>
                <th>Description_post</th>
                <td>{{ post.getDescriptionPost() }}</td>
            </tr>
		</tbody>
	</table>
	<a href="{{ path('post_index') }}">back to list</a>

    <a href="{{ path('post_edit', {'id': post.id}) }}">edit</a>

    {{ include('post/_delete_form.html.twig') }}
{% endblock %}
