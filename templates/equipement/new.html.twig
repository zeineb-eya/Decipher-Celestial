{% extends ('baseback/base.html.twig') %}
{% block body %}
<!DOCTYPE html>

<title>New Equipement</title>
    <h1>Create new Equipement</h1>

    {{ include('equipement/_form.html.twig') }}

    <a href="{{ path('equipement_index') }}">back to list</a>
{% endblock %}
