<form method="post" action="{{ path('localisation_delete', {'id': localisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ localisation.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
