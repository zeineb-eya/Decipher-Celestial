<form method="post" action="{{ path('billet_delete', {'id': billet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ billet.id) }}">
    <button class="btn">Delete</button>
</form>
