<form method="post" action="{{ path('planinng_delete', {'id': planinng.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planinng.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
