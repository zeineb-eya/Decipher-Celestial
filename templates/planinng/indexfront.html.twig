{% extends ('basefront/base.html.twig') %}
 {% block body %}

     <!-- Next Event Section-->
    <section class="bg-primary-shade text-white py-5" id="myTable">
      <div class="container text-center">
        <div class="row align-items-center">
          <div class="col-lg-3 text-lg-end mb-4 mb-lg-0">
            <h2 class="mb-0 text-uppercase">Next Event</h2>
          </div>
          <div class="col-lg-6 text-center mb-4 mb-lg-0">
            <div class="p-2 counter event-counter d-flex align-items-center justify-content-center text-center" data-counter=".event-counter" data-date="Sat Mar 08 2022 11:12:29 GMT">
              <div class="day counter-item text-center mx-3">
                <p class="h2 num"></p><span class="word text-serif">Days</span>
              </div>
              <div class="hour counter-item text-center mx-3">
                <p class="h2 num"></p><span class="word text-serif">Hours</span>
              </div>
              <div class="min counter-item text-center mx-3">
                <p class="h2 num"></p><span class="word text-serif">Minutes</span>
              </div>
              <div class="sec counter-item text-center mx-3">
                <p class="h2 num"></p><span class="word text-serif">Seconds</span>
              </div>
            </div>
          </div>
          <div class="col-lg-3 text-lg-end"><a class="btn btn-outline-light px-4" href="text.html">Learn more</a></div>
        </div>
      </div>
    </section>
    <!-- Events Section-->
    <section class="py-7">
      <div class="container py-5">
        <header class="mb-4 text-center mb-5">
          <h2 class="text-uppercase">Next Events</h2>
        </header>

    
 {% for planinng in planinngs %}
        <div class="p-4 p-lg-5 text-center">
          <div class="row gy-7 align-items-center">
            <div class="col-lg-3">
              <div class="d-flex align-items-center justify-content-center justify-content-lg-start">
                <p class="text-gray text-xl h3 mb-2">{{ planinng.dateDebutPlanning ? planinng.dateDebutPlanning|date('d') : '' }}</p>
                <div class="ms-2">
                  <h5 class="mb-0">{{ planinng.dateDebutPlanning ? planinng.dateDebutPlanning|date('m') : '' }}</h5>
                  <p class="text-small mb-0 text-muted">{{ planinng.dateDebutPlanning ? planinng.dateDebutPlanning|date('Y') : '' }}</p>
                </div>
              </div>
            </div>
            <div class="col-lg-5 text-center">
              <h4 class="text-primary mb-1"> <a class="reset-anchor" href="text.html">{{ planinng.nomPlanning }}</a></h4>
              <p class="text-serif mb-0 text-muted">{{ planinng.dateDebutPlanning ? planinng.dateDebutPlanning|date('Y-m-d') : '' }}</p>
              <p class="text-serif mb-0 text-muted">{{ planinng.dateFinPlanning ? planinng.dateFinPlanning|date('Y-m-d') : '' }}</p>
            <p class="text-serif mb-0 text-muted">{{ planinng.destinationPlanning }}</p>
            

            
            </div>
            <div class="col-lg-3 text-lg-end"><a class="btn btn-outline-primary px-4" href="{{ path('planinng_showfront', {'id': planinng.id}) }}">Détails</a></div>

          </div>
          
        </div>
      </div>
      			{% else %}
				<tr>
					<td colspan="5">no records found</td>
				</tr>
			{% endfor %}
    </section>
    </tbody>





    {% endblock %}